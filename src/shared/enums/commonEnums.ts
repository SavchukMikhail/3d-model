export enum RouterPathEnum {
  Main = '/',
  Scene = '/scene',
  Dashboard = '/dashboard',
  Auth = '/auth',
}

export enum DragNDropEvents {
  DragEnter = 'dragenter',
  DragLeave = 'dragleave',
  DragOver = 'dragover',
  Drop = 'drop',
}
